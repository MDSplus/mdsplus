include @top_srcdir@/Makefile.inc

@MINGW_TRUE@ IMPLIB=@MAKELIBDIR@MdsServerShr.dll.a

SOURCES = \
      ServerDispatchAction.c\
      ServerFreeDispatchTable.c\
      ServerSetDetailProc.c\
      ServerAbortServer.c\
      ServerDispatchClose.c\
      ServerGetInfo.c\
      ServerSetLogging.c\
      ServerBuildDispatchTable.c\
      ServerDispatchCommand.c\
      ServerMonitorCheckin.c\
      ServerStartServer.c\
      ServerCloseTrees.c\
      ServerDispatchPhase.c\
      ServerStopServer.c\
      ServerCreatePulse.c\
      ServerFindServers.c\
      ServerSendMessage.c\
      ServerQAction.c

OBJECTS = $(SOURCES:.c=.o)
EXPORTS = servershr.export

all: @MAKELIBDIR@ @MAKESHLIBDIR@libMdsServerShr@SHARETYPE@ $(IMPLIB) @MAKELIBDIR@libMdsServerShr.a

clean:
	@ $(RM) $(OBJECTS)
	@ $(RM) so_locations
	@ $(RM) @MAKESHLIBDIR@libMdsServerShr@SHARETYPE@ $(IMPLIB)
	@ $(RM) @MAKELIBDIR@libMdsServerShr.a

depend:
	 @makedepend -- $(CFLAGS) -- $(SOURCES)

install: $(libdir)
	$(INSTALL) -m 755  @MAKESHLIBDIR@libMdsServerShr@SHARETYPE@ $(IMPLIB) $(libdir)
	if test "@SHARETYPE@" != ".a" ; then \
		$(INSTALL) -m 644  @MAKELIBDIR@libMdsServerShr.a $(libdir); \
	fi;

@MINGW_TRUE@ MAKE_IMPLIB=-Wl,--out-implib,$(IMPLIB)
@MAKESHLIBDIR@libMdsServerShr@SHARETYPE@: $(OBJECTS) 
	$(LINK.o) $(OUTPUT_OPTION) @LINKSHARED@ $^ -L@MAKESHLIBDIR@ -lMdsdcl -lTdiShr -lTreeShr -lMdsShr -lMdsIpShr $(LIBS) @SOCKETLIB@ $(THREAD) $(MAKE_IMPLIB)

@MAKELIBDIR@libMdsServerShr.a: $(OBJECTS)
	$(AR) -cr $@ $^
	@RANLIB@ $@

