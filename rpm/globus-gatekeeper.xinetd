service gsigatekeeper
{
      socket_type  = stream
      protocol     = tcp
      wait         = no
      user         = root
      server       = /usr/local/fusiongrid/sbin/gsigatekeeper
      disable      = no
}
