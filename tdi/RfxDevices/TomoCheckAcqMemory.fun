public fun TomoCheckAcqMemory(in _freq, in _duration, in _maxSamples)  
{
return(1);
/*
	write(*, _duration);

	if( data(_freq) * data(_duration) > data(_maxSamples))
		return (0);
	return (1);
	*/
}
