CFLAGS = -c -I../include -I. -g

OBJECTS = \
	main-uil.o \
	bxutils-uil.o \
	CallbacksUil.o \
        ReadInt.o \
        ReadString.o \
        GetNciString.o \
        GetSupportedDevices.o

all : ../bin/traverser \
	../uid/traverser.uid

../bin/traverser : $(OBJECTS) ../bin
	cc -o $@ $(OBJECTS)  -L../shlib -lMdsShr -lTreeShr -lTdiShr -lXmdsShr -lMrm -lXm -lXt -lX11 

../bin :
	mkdir ../bin

../uid/traverser.uid : traverser.uil
	uil -wmd ../shlib/xmdsuil.wmd -o $@ $<
	
clean :
	- rm *.o
	- rm ../bin/traverser
	- rm ../uid/traverser.uid
	- rm .depend
	- rm *~

depend : .depend

.depend :
	- rm .depend
	find *.c -exec cc -DMDS_EXTENSION -I../include -I.  -M {} > .depend \;

include .depend

