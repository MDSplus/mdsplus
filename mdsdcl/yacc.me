#!/bin/sh
# bison -V	# bison (GNU Bison) 3.0.2
# flex -V	# flex 2.5.39

cd $(dirname $0)
ycmd='bison -y cmdParse.y -bcmdParse'
xcmd='for f in *.x ; do flex $f;done'
scmd='sed -i "s/int num_to_read/size_t num_to_read/g" mdsdclDeltatimeToSeconds.c'
eval "$ycmd"
eval "$xcmd"
eval "$scmd"
